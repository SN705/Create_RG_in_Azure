name: Azure Resource Group Deployment
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_DINO_SMILES_CREDENTIALS }}
      - uses: azure/bicep@v1
        with:
          args: build ResourceGroup
      - uses: azure/cli@v1
        with:
          inlineScript: |
            az deployment sub create --name RGDeployment1 --location eastus --template-file ResourceGroup
